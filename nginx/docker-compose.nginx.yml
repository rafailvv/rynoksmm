networks:
  mynetwork:


services:
  nginx:
    image: nginx:latest
    container_name: nginx_reverse_proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - nginx_data:/nginx

    networks:
      - mynetwork

volumes:
  nginx_data: